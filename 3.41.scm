;; I don't agree with Ben. Unlike deposit and withdraw, when 'balance is
;; supplied, balance returns a number, which is *primitive*, and therefore can
;; not be interrupted, so there's no need to protect it
